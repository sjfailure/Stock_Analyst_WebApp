I did some logging and I've found a weird and unexplainable problem. When I run the data_wrangling_for_main() function (and it's helpers) in a Python instance in Powershell, they work correctly, gather the desired data and everything is fine. When I run the Django server and make the URL call to that activates the function, I consistently get an error.

This is a helper function to data_wrangling_for_main():
def get_company_id(company_name):
    print(f"TEST LOG get_company_id(): company_name={company_name}")
    query = "SELECT id FROM Companies WHERE company_name=?"
    answer = select_query(query, (company_name,))
    print(f'TEST LOG get_company_id(), answer = {answer}')
    if not answer:
        raise ValueError(f'Database yielded not data for query "{query}" ?="{company_name}"')
    return answer[0][0]


when data_wrangling_for_main() or this function are run independently, it works just fine. When the server is running, the custom ValueError is consistently triggered. What are your thoughts? I have an idea but don't want to taint your initial speculation.