Let's undo the veil a bit and see if it helps. The base directory if "Stock_Analyst_Webapp." Within it are four subdirectories: "model," "mvp_stock_app," "staticfiles," and "stock_analyst." "stock_analyst" is the core app of the project and "mvp_stock_app" is the Django project directory. I chose to handle all database functionality manually, separating the model from the views (and controller), putting all database and API administration materials in the "model" directory.
The time has come to start to marry to two pieces by connecting the model to the controller and projecting views from it. The function "get_highs_for_main_view()" is in the DatabaseAdmin.py file in the model directory. I'm trying to access this function in the "stock_analyst/views.py" file so that the template view can call data from the model. Surely there is an easy way to achieve this.